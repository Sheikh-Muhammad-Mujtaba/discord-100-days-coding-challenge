// Create an array to store different types of vehicles
let transports: string[] = ["Toyota Supra", "Nissan GTR", "Honda 125cc Motorbike"];

// Use the forEach loop to iterate through each transport type
transports.forEach(transport => {
    console.log(`I would like to own a ${transport}.`);
});
