let direction: "left" | "right" | "up" | "down"; // Declaring a variable 'direction' that can hold one of four string literals

function setColor(color: "red" | "green" | "blue") {
    // Function to set color, accepting only specific string literals "red", "green", or "blue"
    // Implementation details omitted for brevity
}
