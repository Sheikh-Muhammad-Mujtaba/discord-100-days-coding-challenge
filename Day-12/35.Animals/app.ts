// Define an array of animal names
let animals: string[] = ["elephant", "dolphin", "hawk", "octopus", "dog"];

// Iterate over each animal
animals.forEach(animal => {
    console.log(`${animal} has sensory organs for detecting stimuli.`);
});

