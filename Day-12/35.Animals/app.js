// Define an array of animal names
var animals = ["elephant", "dolphin", "hawk", "octopus", "dog"];
// Iterate over each animal
animals.forEach(function (animal) {
    console.log("".concat(animal, " has sensory organs for detecting stimuli."));
});
