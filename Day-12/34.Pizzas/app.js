// Define an array of pizza types
var pizzas = ["pepperoni", "margherita", "hawaiian", "Vegetarian", "Supreme"];
// Iterate over each pizza type using forEach
pizzas.forEach(function (pizza) {
    // Print a custom message expressing your preference for each pizza
    console.log("I like ".concat(pizza, " pizza! \uD83C\uDF55"));
});
