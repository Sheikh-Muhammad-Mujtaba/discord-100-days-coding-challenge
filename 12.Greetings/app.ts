// Create an array to store the names of friends
let names: string[] = ["Umair", "Sulyman", "Abdullah"];

// Use a loop to greet each friend and invite them to exercise
for (let name of names) {
    console.log(`Hey, ${name} would you like to do some exercise with me?`);
}
