// Create an array to store the names of friends
let names: string[] = ["Umair", "Sulyman", "Abdullah"];

// Use a loop to print each name in sequence
for (let i = 0; i < names.length; i++) {
    console.log(names[i]); // Print the current name to the console
}
